
- Add Hunt table so that everything is tied to it allowing for multiple hunts
- Adjust size of image to fit fully no matter what it is - scale it 
- The last QR code should lead to a congratulations site with a leaderboard
- add a copy sign next to the recovery ID
- change the title so it's not just "QR Scavenger Hunt  Sequential QR scavenger hunt" but the name of the Hunt under "QR Scavenger Hunt"

- Add an edit station section in the admin dashboard should be with the 
- Add photo upload to admin dashboard in create and edit station
- Add QR code generation from admin dashboard
- Adding a station with a station order that already exists should show an error that reflects that, not "Could not generate a unique station code."

- Remove the station_scans table as it is unnecessary

- Make the scan be the points. Revealing anything is just to get info/hints to the next station - be that an image or text
- Failed read attempts or reloading the site to '.' should lead back to your station and in the case of a bad read (too early) with a message that says that that's the wrong station - or not yet, or smth like that
- Make any scans of old stations lead to their current one as well
- The Show hint again button shouldn't show the modal window with "Are you sure? This hint costs 3 points." It should just show the hint
- After registering the site shouldn't show "Start  QR Scavenger Hunt  Register your team, scan the first QR code, and complete the stations in order." anymore. It should just show the first hint or station 0 (for no points) with the recovery code message above it 
- add optional sound hint

- qr scanner right there
